apiVersion: v1
kind: Service
metadata:
  name: ssprint0
  labels:
    app.kubernetes.io/name: ssprint0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ssprint0
  ports:
    - name: http
      port: 80
      targetPort: http
